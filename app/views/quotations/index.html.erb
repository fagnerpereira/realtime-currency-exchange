<p id="notice"><%= notice %></p>


<div class="flex justify-center items-center w-full h-screen bg-indigo-50">
  <div class="flex flex-col bg-white w-full">
    <div class="text-center text-2xl">Currency Exchange in Realtime</div>

    <%= turbo_stream_from :quotations %>
    <!-- graph container -->
    <div class="flex justify-start items-end space-x-2" id="quotations">
      <%= render @quotations %>
    </div>
  </div>
</div>
